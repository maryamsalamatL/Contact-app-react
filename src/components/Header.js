import { Link } from "react-router-dom";

const Header = () => {
  return (
    <header>
      <div className="header">
        <h1>Contact Manager</h1>
      </div>
    </header>
  );
};

export default Header;
